<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.evo.evoproject.mapper.order.OrderMapper">

    <insert id="insertOrder" parameterType="com.evo.evoproject.domain.order.Order">
        INSERT INTO orders (order_no, user_no, pro_no, order_name, order_address_1, order_address_2, order_phone, order_comment, pro_name, order_timestamp, pro_stock, order_payment, order_status, request_type, order_delivnum)
        VALUES (#{order_no}, #{user_no}, #{pro_no}, #{order_name}, #{order_address1}, #{order_address2}, #{order_phone}, #{order_comment}, #{pro_name}, #{order_timestamp}, #{pro_stock}, #{order_payment}, #{order_status}, #{requestType}, #{order_delivnum})
    </insert>

    <select id="findAllOrders" parameterType="int" resultType="com.evo.evoproject.domain.order.Order">
        SELECT * FROM orders WHERE user_no = #{user_no}
    </select>

    <select id="getOrderById" parameterType="java.lang.Integer" resultType="com.evo.evoproject.domain.order.Order">
        SELECT * FROM orders WHERE user_no = #{user_no}
    </select>

    <update id="updateOrder" parameterType="com.evo.evoproject.domain.order.Order">
        UPDATE orders SET order_name = #{order_name}, pro_no = #{pro_no}, order_address_1 = #{order_address1}, order_address_2 = #{order_address2}, order_phone = #{order_phone}, order_comment = #{order_comment}, pro_name = #{pro_name}, order_timestamp = #{order_timestamp}, pro_stock = #{pro_stock}, order_payment = #{order_payment}, order_status = #{order_status}, request_type = #{requestType}, order_delivnum = #{order_delivnum}
        WHERE order_no = #{order_no}
    </update>

    <delete id="deleteOrder" parameterType="java.lang.Integer">
        DELETE FROM orders WHERE order_no = #{order_no}
    </delete>

</mapper>
